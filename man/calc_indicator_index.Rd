% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_indicator_index.R
\name{calc_indicator_index}
\alias{calc_indicator_index}
\title{Normalize indicator values to a reference (base = 100)}
\usage{
calc_indicator_index(data, value_col, group_col, ref_value)
}
\arguments{
\item{data}{A data frame or tibble containing the indicator values.}

\item{value_col}{Character string naming the column with indicator values
to normalize (e.g. \code{"gammadiv"}).}

\item{group_col}{Character string naming the grouping column
(e.g. \code{"year"}).}

\item{ref_value}{The value of \code{group_col} to use as the reference
(e.g. \code{2012}). The indicator value for this group becomes 100.}
}
\value{
The input data frame with an additional column \code{index}, where
each value is \code{(value / reference_value) * 100}.
}
\description{
Takes a summary table (e.g. output of \code{\link{calc_gammadiv}}) and
normalizes a value column relative to a reference group, producing an
index where the reference equals 100.
}
\details{
This is a simple normalization commonly used to compare temporal changes
in biodiversity indicators. The reference group (e.g. the first survey
year) is set to 100 and all other values are expressed relative to it.
}
\examples{
gamma <- calc_gammadiv(mzb, group_by_col = "year")
calc_indicator_index(gamma, "gammadiv", "year", ref_value = 2012)

}
