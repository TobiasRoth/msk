% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fisch.R
\docType{data}
\name{fisch}
\alias{fisch}
\title{Fish data (events and occurrences)}
\format{
A \code{list} with 2 elements:
\describe{
\item{event}{A tibble with 176 rows and 6 variables:
\describe{
\item{eventID}{Character; unique identifier of the sampling event
(e.g. \code{"CH_002_BS_FISH-41134"}).}
\item{locationID}{Integer; identifier of the sampling location
(NAWA station number).}
\item{year}{Integer; year of sampling.}
\item{altitude}{Numeric; altitude of the sampling location in
metres above sea level.}
\item{stream}{Character; name of the sampled stream.}
\item{N_runs}{Integer; number of electrofishing runs per event.}
}
}
\item{occurrence}{A tibble with 1,109 rows and 6 variables:
\describe{
\item{eventID}{Character; link to the corresponding entry in
\code{event}.}
\item{taxonID}{Numeric; Swiss species code (NUESP) of the recorded
species.}
\item{individualCount}{Integer; total number of individuals
recorded per species and event (summed across electrofishing runs).}
\item{totalWeight_g}{Numeric; total weight in grams per species and
event (summed across individuals; \code{NA} where weight was not
recorded).}
\item{scientificName}{Character; scientific name of the species.}
\item{vernacularName}{Character; German common name of the species.}
}
}
}
}
\usage{
fisch
}
\description{
Dataset with sampling events and associated species occurrences
(fish) from the Swiss NAWA monitoring programme.
The data are stored as a list with two tibbles following the
\href{https://dwc.tdwg.org/terms/}{Darwin Core} standard:
\code{event} describes individual sampling events, \code{occurrence}
the observed species per event. The dataset is pre-filtered to the four
NAWA campaigns, sites below 1000 m a.s.l. sampled in at least three
of the four campaigns.
}
\details{
The two tibbles are linked by \code{eventID}: each row in
\code{occurrence} refers to exactly one event in \code{event}.
The dataset covers the four NAWA campaigns (2012, 2015, 2019, 2023),
restricted to sites below 1000 m a.s.l. with data in at least three
of the four campaigns (51 stations, 40 species). Fish were sampled by
electrofishing; \code{N_runs} records the number of passes per event
and can be used to standardise abundance estimates.
The \code{totalWeight_g} column enables biomass analyses; note that
weight data are missing for approximately 27\\% of individual records.

\strong{Important -- for demonstration purposes only.} This dataset is
included solely to illustrate package functionality. It must not be used
for analysis, reporting, or publication. The authoritative source for
Swiss NAWA fish data is the
\href{https://www.fischnetz.ch}{fischnetz} database and the relevant
cantonal and federal data repositories. In case of any discrepancy
between this dataset and the authoritative sources, those sources take
precedence.
}
\examples{
data("fisch")
names(fisch)
dplyr::glimpse(fisch$event)
dplyr::glimpse(fisch$occurrence)

}
\keyword{datasets}
